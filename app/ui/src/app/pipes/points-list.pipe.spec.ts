import { PointsListPipe } from './points-list.pipe';

describe('PointsListPipe', () => {
  it('create an instance', () => {
    const pipe = new PointsListPipe();
    expect(pipe).toBeTruthy();
  });
});
